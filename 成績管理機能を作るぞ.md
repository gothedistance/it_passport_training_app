
# 成績管理の機能でやらないといけないこと
- [ ] 成績の登録
- [ ] 成績の一覧表示
    - [ ] AppBarの右上にアイコンでもつけてもらって、それをタップすると、回答履歴の一覧が表示されるようにする
    - [ ] 一覧表示には `GridView`というWidgetを使ってください！

## 成績データの保存方法について
- アプリが落とされた時や、スマホの再起動があった時でも、回答履歴は復元できる必要があります。
- アプリの更新があって適用したら、回答履歴が消えた瞬間に、AppStoreのレビューで炎上するぞ！
- なので、アプリのデータは、アプリの外に保存する必要があります。

## 回答履歴のデータ構造
- 絶対的な正解があるわけじゃないんで、仕様が満たせればなんでもいい。基本は。

```json
[
    {
        "version": "2020",
        "history":  [ 
            {"no": 1, "answer": "ア", "correct": "エ",  },
            {"no": 2, "answer": "ア" ,"correct": "エ",  },
            {"no": 3, "answer": "ア" ,"correct": "エ",  },
        ]
    }
]
```

## アプリの外にデータを保存する方法
- アプリの外にデータを保存する方法は、いくつかありますが、最も簡単なのは、`SharedPreferences`を使うことです。
- `SharedPreferences`は、アプリの外にデータを保存するためのAPIです。

### `SharedPreferences`の使い方
- https://pub.dev/packages/shared_preferences
```dart
final SharedPreferences prefs = await SharedPreferences.getInstance();
// 回答履歴の保存
await prefs.setString('history', 'JSON形式の文字列');

// 保存したデータを取得する
// 取得した文字列は、jsonDecodeでオブジェクトにして、クイズ一覧同様に型を与えましょう！
String? history = prefs.getString('history');

if (history == null) {
    return [];
} else {
    final jsonData = jsonDecode(history);
    final quizList = jsonData.map((e) => Quiz.fromJson(e)).toList();
    return quizList;
}
```

## ロジックの実装
###　回答の保存する
- 回答ボタンを押した時に、何問目に何を回答したかという状態を受け取って、それをSharedPreferenceに保存しましょう。
- 1問目の解答、2問目の解答、3問目...という形で、データが増加していきます。`history`の配列に追加する形にならないといけないんで、頑張って保存してください。

### 回答履歴の表示
- これはサンプルのコードのまんまです。
    - `shared_preferences`に保存した文字列をゲット
    - `jsonDecode`でデコード
    - `history`のデータ構造に合わせて`fromJson`して、型をつけたデータにする
    - `FutureBuilder`をつかい、データを取得して、`GridView`で表示する。


